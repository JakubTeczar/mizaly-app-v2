node_modules/
dist/
build/
.env
!.env.example
*.log
.DS_Store
.vscode/
coverage/

# Prisma
apps/backend/prisma/dev.db

# PWA build artifacts
apps/mobile/dist/
apps/admin/dist/

# Docker
postgres-data/

# Locally re-hosted scraped images (see apps/backend/src/lib/localImageStore.ts)
apps/backend/storage/

# Python bytecode cache (apps/instagram-scraper)
__pycache__/
*.pyc

# DB dumps - contain live OAuth tokens (SocialAccount) + password hashes, never commit.
# Scoped to the repo root only (not */prisma/migrations/*/migration.sql, which must stay tracked).
/*.sql
/*_dump*.sql
