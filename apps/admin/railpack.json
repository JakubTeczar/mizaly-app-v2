{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["yarn install --ignore-scripts --frozen-lockfile", "yarn workspace @mizaly/admin build"]
    }
  },
  "deploy": {
    "startCommand": "npx --yes serve -s apps/admin/dist -l $PORT"
  }
}
